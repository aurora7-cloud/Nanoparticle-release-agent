services:
  - type: web
    name: nanocarrier-release-agent
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python src/rf_shap_web_app.py
    autoDeploy: true

